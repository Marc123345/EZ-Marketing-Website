{
  "name": "ez-marketing",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs && npx --yes serve -l 5270 dist",
    "build:hero": "esbuild src/hero3d/index.js --bundle --minify --format=iife --target=es2020 --outfile=src/assets/js/hero3d.js"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@dimforge/rapier3d-compat": "^0.14.0",
    "esbuild": "^0.28.2",
    "three": "^0.183.2"
  }
}
